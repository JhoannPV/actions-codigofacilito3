name: Deploy

on: push

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Print variable
              run: echo "${{ secrets.TOKEN_SECRECT }}"

            - name: setup
              uses: actions/setup-node@v4
              with:
                node-version: '18'